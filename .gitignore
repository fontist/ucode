/data/
/output/
/site/
/.bundle/
/pkg/
/coverage/
/tmp/
/doc/
/.yardoc/
/.rspec_status

# UCD text files, Unihan, and per-block PDFs are downloaded on first
# use via `bin/ucode fetch` into data/. Never commit production UCD
# assets — only the small fixture slices under spec/fixtures/ucd/.

# Bundler local config
/.bundle/config

Gemfile.lock

# Planning docs and scratch notes — local working tree only, never in git.
TODO*
BUG-*.md
REQ-*.md

# Built gem artifacts.
*.gem

